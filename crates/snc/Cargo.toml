[package]
name = "snc"
version.workspace = true
edition = "2021"
description = "SNL compiler CLI for sequencer-rs"

[dependencies]
snc-core = { workspace = true }
clap = { version = "4", features = ["derive"] }
