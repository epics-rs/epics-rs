[package]
name = "rtems-exec-gate"
version = "0.0.0"
edition = "2024"
publish = false
license = "EPICS"
description = "Dev-only: the rtems-exec-model test-gating census, shared by every crate in the RTEMS closure"

[dependencies]
