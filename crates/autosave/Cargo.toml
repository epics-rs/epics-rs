[package]
name = "autosave-rs"
version = "0.2.1"
edition = "2021"
description = "EPICS autosave ported to Rust"

[dependencies]
epics-base-rs = { path = "../epics-base" }
tokio = { version = "1", features = ["full"] }
thiserror = "2"
chrono = "0.4"

[dev-dependencies]
tempfile = "3"
