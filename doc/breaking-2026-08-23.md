# BREAKING public API changes — v0.26.2 (7b4f7f9a) to the 2026-08-23 parity round

Measured, not reported: every `pub` item (excluding `pub(crate)`/`pub(super)`)
in `crates/*/src/*.rs` extracted at both revisions and joined on
`file :: name`. Base 6666 public items, head 6801.

## Removed (18)

| item | file |
|---|---|
| `open_stream_eager` | ad-core-rs/src/plugin/file_base.rs |
| `take_captured` | ad-plugins-rs/src/circular_buff.rs |
| `resolve_request_file` | epics-base-rs/src/server/autosave/startup.rs |
| `Decimate::every` | epics-base-rs/src/server/database/filters/decimate.rs |
| `SyncMode::from_keyword` | epics-base-rs/src/server/database/filters/sync.rs |
| `load_substitution_file` | epics-base-rs/src/server/db_loader/substitution.rs |
| `bits`, `from_bits` | epics-base-rs/src/server/recgbl.rs |
| `park_notify_put`, `put_notify_busy` | epics-base-rs/src/server/record/record_instance.rs |
| `ProcessMode`, `PutOptions`, `dbe_mask_from_pv_request`, `from_pv_request` | epics-bridge-rs/src/qsrv/channel.rs |
| `monitor_mask` | epics-pva-rs/src/nt/qsrv_marks.rs |
| `SourceEntry` | epics-pva-rs/src/server_native/composite.rs |
| `last_transaction_id` | modbus-rs/src/interpose.rs |
| `set_ca_trig_pending` | std-rs/src/records/epid.rs |

`ProcessMode` is not deleted — it moved to `epics-base-rs`
`server::database::field_io::ProcessMode` as the one enum behind both the
PVA sources and the QSRV group put.

## Signature changed (24; the non-cosmetic ones)

| item | before | after |
|---|---|---|
| `NATIVE_VALUE_BYTES` | `= 40` | `= 8` |
| `recgbl::get_time_stamp` | `-> SystemTime` | `-> Option<SystemTime>` |
| `recgbl::rec_gbl_check_udf` | `(common, exact_one, msg)` | `(common, exact_one, severity: Option<AlarmSeverity>, msg)` |
| `recgbl::rec_gbl_set_sevr` | `-> ()` | `-> bool` (C's raise) |
| `recgbl::rec_gbl_set_sevr_raw` | `-> ()` | `-> bool` |
| `general_time::get_event` | `-> SystemTime` | `-> Option<SystemTime>` |
| `log::errlog_sev_printf` | `-> bool` | `-> ()` |
| `format::format_json` | `(pv_name, value)` | `(pv_name, value, marked: Option<&HashSet<String>>)` |
| `format::format_raw` | `(pv_name, desc, value)` | `(pv_name, desc, value, marked)` |
| `PactExit` | tuple struct `(Option<DeferredNotifyPut>)` | named-field struct |
| `autosave::format_verify_report` | `(&[VerifyEntry])` | `(&VerifyReport)` |
| `filters::dbnd::new` | `(threshold, mode)` | `(cval, mode)` |
| `filters::dbnd::relative` | `(fraction)` | `(percent)` |
| `filters::decimate::new` | `(n, offset) -> Self` | `(n) -> Option<Self>` |
| `filters::sync::new` | `(mode, state_name: impl Into<String>)` | `(mode, state: Arc<DbState>)` |
| `catool::caput` | `-> PutOutcome` | `-> Result<PutOutcome, ToolError>` |
| `catool::caput_array` | `-> PutOutcome` | `-> Result<PutOutcome, ToolError>` |
| `attributes::PvAttribute::new` | `(pv_name)` | `(pv_name, dbr_type: PvAttrDbrType)` |
| `interpose::frame_request` | `-> ModbusResult<Vec<u8>>` | `-> ModbusResult<FramedRequest>` |
| `interpose::Interpose::new` | `(link_type)` | `()` |
| `ioc::*::new` | `(handle)` | `(handle, link: LinkType)` |
| `ioc::with_write_delay` | `(handle, write_delay)` | `(handle, write_delay, link)` |
| `asyn port_handle::wait_blocking` | `(_timeout)` ignored | `(timeout)` honoured |
| `EpidSoftCallbackDeviceSupport` | braced struct | unit struct |
