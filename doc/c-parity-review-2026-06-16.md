# Workspace C-parity review — 2026-06-16 (round 4: commit-history-classified families)

Methodology note: rounds 1–3 were static C-vs-Rust source sweeps (now
exhausted/closed). This round classifies the **commit history** since
`v0.15.0` (2411 commits, 1755 `fix`/`feat`) into the *recurring* structural
families — the empirically-proven bug families, the same basis the
`examples/regression-ioc` harness was built on — and cross-references each
family's coverage status to find which still have **open siblings**. A
targeted code hunt (4 parallel read-only agents) confirmed the open siblings
at file:line.

## Parity philosophy (scope filter — unchanged)

Only OUTPUT FORM must match C/pvxs (wire/DBR/PVA encodings, on-disk dataset
shape, observable record-field values incl. DBE masks). Internal design may
differ if observably equivalent. A divergence counts ONLY if observably
different AND reachable.

## Recurring-family classification (commit-history theme frequency vs coverage)

| Recurring family | history commits | coverage status |
|---|---|---|
| disconnect / teardown | 41 | trap-write `AfterWrite` on cancel/supersede/teardown **FIXED (R4-4, 02053a3d)** |
| String / type-drop | 34 | common-field FIXED (49942183), P-array FIXED; compress INP-link **FIXED (R4-3, f388beab)** |
| timestamp / utag | 33 | pinned (regr H), utag FIXED (regr Q) — closed |
| init / load order | 30 | pinned (regr O), common-field-load FIXED — closed |
| flow-control credit | 30 | audited sound (pipeline credit + dropped-monitor owner) — closed |
| link locality / PP | 28 | non-local Db-link family closed — closed |
| numeric convert | 26 | ADP-56 signed off; QSRV String→int radix **FIXED (R4-2, 90167988)** |
| monitor DBE-mask | 24 | pinned (regr I/M), SCAL-1/2 closed — closed |
| menu / enum label | 16 | enum *serving* pinned (regr E/F/R); db-load label resolution **FIXED (R4-1, e7e20583)**; runtime DBR_STRING-write label resolution **FIXED (R4-5, 86137c83)** |
| alarm / severity | 16 | pinned (regr G/N) — closed |
| array / waveform | 15 | P-family FIXED — closed |

7 of 11 families fully covered; the 4 with open siblings (R4-1..R4-4 below) are now **all FIXED** (R4-1 e7e20583, R4-2 90167988, R4-3 f388beab, R4-4 02053a3d; plus the R4-5 write-path sibling 86137c83).

## Open Findings

### R4-1: db-load menu-label resolution uses a global table, not the field's own menu — 12 menus drop, sel `SELM` mis-maps
Severity: High — **FIXED (e7e20583)**

Root cause (structural): `db_loader::apply_fields` (`crates/epics-base-rs/src/server/db_loader/mod.rs:972-979`) parses a field-list menu field's `.db` value with `EpicsValue::parse(desc.dbf_type, value_str)`, whose only label path is the hand-maintained global `resolve_menu_string` (`crates/epics-base-rs/src/types/value.rs:1075-1142`). It ignores the record's own complete, correctly-ordered `menu_field_choices` table (`record_trait.rs:339`, per-record overrides). A menu label is **menu-specific**, so a single global table is fundamentally wrong: it (a) drops labels it doesn't know → the record load errors out, and (b) mis-maps shared labels to the wrong index.

Confirmed drops (`field(<field>, "<Label>")` fails the record load), each with the canonical labels living in an *existing* record constant the loader ignores:
- ai/ao `LINR` (menuConvert, 15 labels: `NO CONVERSION`/`SLOPE`/`LINEAR`/typeK… ) — `ai.rs:137`, `ao.rs:258`
- compress `ALG` (compressALG, 6) — `compress.rs:351`, `COMPRESS_ALG_CHOICES`
- compress `BALG` (bufferingALG, 2) — `compress.rs:376`, `COMPRESS_BALG_CHOICES`
- histogram `CMD` (histogramCMD, 4: Read/Clear/Start/Stop) — `histogram.rs:181`
- ao `OIF` (aoOIF, 2: Full/Incremental) — `ao.rs:308`, `AO_OIF_CHOICES`
- swait `OOPT` missing `Never` (7th synApps choice) — `swait.rs:121`
- swait `DOPT` (Use VAL/Use DOL) — `swait.rs:128`, `SWAIT_DOPT_CHOICES`
- scalcout `OOPT` missing `Never` — `scalcout.rs:182`
- transform `COPT` (Conditional/Always) — `transform.rs:109`
- transform `IVLA` (Ignore error/Do Nothing) — `transform.rs:115`
- sseq `WAIT1..WAITA` (sseqWAIT, 12: NoWait/Wait/After1..AfterA) — `sseq.rs:870`

Confirmed mis-map (worse — silent wrong value, no error):
- sel `SELM "Specified"` → `resolve_menu_string` returns **1** (from the menuFanout `All/Specified/Mask` block, `value.rs:1111`), but selSELM order is `Specified(0)/High Signal(1)/Low Signal(2)/Median Signal(3)` (`sel.rs:11` `SELM_CHOICES`). So `field(SELM,"Specified")` loads SELM=1 ("High Signal"). `sel.rs:125`.

C ref: dbStaticLib resolves a menu field's `.db` value against that field's own `menuFtype`/record menu (`/Users/stevek/codes/epics-base/modules/database/src/ioc/dbStatic/dbStaticLib.c` `dbPutString`/`dbGetMenuIndexFromString`), never a cross-menu global table.

Structural fix (done, e7e20583): `apply_fields`, when `record.menu_field_choices(field).or_else(shared_menu_choices)` is `Some(choices)`, resolves the value against the field's own menu — exact label first then a numeric index (matching C order; `dbGetMenuIndexFromString`'s `strcmp` precedes `epicsParseUInt16`) via the new shared `resolve_menu_field_string` (`record/menu_choices.rs`) — instead of `EpicsValue::parse`'s global table. An unknown choice errors (`S_db_badChoice`) instead of mis-mapping. No record edits were needed: every affected record's `menu_field_choices` was already complete. Pinned by `db_loader::tests::db_load_menu_labels_resolve_against_field_menu`.

### R4-2: QSRV PVA String→integer PUT is base-10-only; pvxs uses base-0 (hex/octal)
Severity: Medium — **FIXED (90167988)**
Fix: the two `scalar_to_i64`/`scalar_to_u64` String branches route through the CA path's existing base-0 parsers (`EpicsValue::parse_int`/`parse_uint`, now public). Empty/garbage/out-of-range still reject (pvxs parity); only the accepted set widens to hex/octal. Pinned by `convert::tests::string_to_numeric_put_accepts_c_radix`.
Rust: `crates/epics-bridge-rs/src/convert.rs:229-237` (`scalar_to_i64`), `:264-272` (`scalar_to_u64`) — a `ScalarValue::String` is parsed `s.trim().parse::<i64>()/<u64>()` (decimal-only), rejecting `0x`/octal. Reached from `crates/epics-bridge-rs/src/qsrv/channel.rs:646-651`.
C ref: pvxs `src/util.cpp:786-817` `parseTo<int64_t/uint64_t>` use `std::stoll/stoull(s,&idx,0)` — base-0 (auto hex/octal).
Reachability: `pvput PV "0x1F"` into a numeric field → pvxs writes 31; Rust QSRV returns `PutRejected`. The convert.rs comment already claims pvxs parity, and the CA sibling (`value.rs:1210` `parse_int`/`parse_uint`) already does C-radix — the bridge path is inconsistent with both.

### R4-3: compress `INP`-link delivery drops a non-Double linked source
Severity: Medium — **FIXED (f388beab)**
Fix: `put_field_internal` (the single ReadDbLink-delivery owner) coerces the delivered value to the target field's `dbf_type` (from `field_list`), mirroring C `dbGetLink(DBF_<target>)`. sseq's `put_field_internal` override is the distinct owner for its own targets. Pinned by `compress::pbuf_tests::input_link_coerces_long_source_into_double_buffer`.
Rust: input-link read delivery `crates/epics-base-rs/src/server/database/processing.rs:2730,2785` (`let _ = instance.record.put_field_internal(target, value)` — error discarded); `put_field_internal` (`record_trait.rs:896-908`) coerces only `EnumWithChoices`, not to the target field's DBF type; compress `VAL` arm accepts only Double/DoubleArray (`compress.rs:517-536`).
C ref: `compressRecord.c:342` `dbGetLink(&prec->inp, DBF_DOUBLE, …)` — C requests DBF_DOUBLE, so the link layer converts any numeric/string source to double before the record sees it.
Reachability: a compress record with `field(INP,"SRC.VAL")` where SRC is DBF_LONG (longin/calc) or waveform FTVL=LONG delivers `EpicsValue::Long(Array)` → VAL arm `_ => Err(TypeMismatch)` → discarded → buffer never advances, VAL stays zero. CA/PVA/OUT-link write paths are SAFE (they coerce via `field_io.rs:643-667` / `:101-127`); sseq's two `ReadDbLink` targets are covered by its own override. Structural fix: `put_field_internal` should coerce to the target field's `dbf_type` from `field_list()` (same pattern `put_pv_inner` uses), closing every `ReadDbLink` target by construction.

### R4-4: trapped put-callback `AfterWrite` finalizer skipped on abort/supersede/teardown (3 sites, one family)
Severity: Medium — **FIXED (02053a3d)**
Invariant (MUST): once `asTrapWrite`/`BeforeWrite` is dispatched for a trapped put, exactly one `AfterWrite` MUST follow on every exit path (completion, async-abort, supersede, teardown).
Rust bypassing paths (no RAII finalizer — `AfterWrite` is a plain await-sequenced statement an `abort()` cuts):
1. PVA QSRV blocking put — `crates/epics-bridge-rs/src/qsrv/trap_write.rs:112-118` (Before at :112, `write().await` parks at `channel.rs:701 rx.await`, After at :118); the PUT-EXEC task's `AbortOnDrop` (`epics-pva-rs/src/server_native/tcp.rs:1053`) cuts it on DestroyChannel/teardown. Group path `qsrv/group.rs:1189` same shape.
2. CA server supersede — `crates/epics-ca-rs/src/server/tcp.rs:330` `supersede_put_notify` aborts prev + sends only `ECA_PUTCBINPROG`, never the superseded `AfterWrite`. C fires `asTrapWriteAfter` before that reply (`camessage.c:1697-1701`).
3. CA server connection teardown — `crates/epics-ca-rs/src/server/tcp.rs:3429-3479` dispatches `AfterWrite` only after `rx.await`; connection-drop abort (`tcp.rs:880`) cuts the parked task. C fires it from the disconnect branch (`camessage.c:1619-1620`).
C ref: EPICS base `asTrapWriteAfter` on every cancel branch (`camessage.c:1400/1620/1700`); pvxs `SecurityLogger` RAII destructor (`ioc/securitylogger.h:28-29`).
Observable: a caPutLog/trap-write listener records `BeforeWrite` with no paired `AfterWrite` on supersede/teardown of a slow (motor/async) put — a security-audit-trail divergence vs a real C IOC. Structural fix: a scope/Drop guard owning the Before/After pair (RAII equivalent of `SecurityLogger`), one helper covering all three sites.
Fix (done): new `TrapWriteGuard` + `TrapWriteFields` in `epics-base-rs::server::access_security` — `begin()` dispatches `BeforeWrite`, `complete(status)` dispatches the normal-path `AfterWrite` and disarms, and `Drop` dispatches a cancel `AfterWrite` if still armed (the abort/supersede/teardown paths). `AfterWrite` therefore fires exactly once on every exit path by construction. The bridge `put_with_trap` holds the guard across `write().await`; the CA server moves it into the async completion task so a task `abort()` (supersede via `supersede_put_notify`, teardown via the `write_notify_tasks` drain) runs the guard's `Drop`. The three explicit Before/After dispatch sites are removed for the single owner. Pinned by `access_security::tests::trap_write_guard_{complete_fires_one_after_and_disarms_drop,drop_without_complete_fires_cancel_after}`, `qsrv::trap_write::tests::after_fires_when_write_future_cancelled`, and `server::tcp::put_notify_supersede_tests::supersede_fires_cancel_after_write_via_guard_drop`.

### R4-5: runtime `DBR_STRING` write to a `DBF_MENU` field mis-maps the label (write-path sibling of R4-1)
Severity: Medium — **FIXED (86137c83)**
Root cause (same family as R4-1): the write coercion (`field_io.rs` `put_pv_inner` :120, `put_pv_and_post_with_origin` :342, `put_record_field_from_ca_inner` :660) coerced a type-mismatched value with `EpicsValue::convert_to(target)`, whose `String→Short/Enum` path (`value.rs:932/937`) consults the field-blind global `resolve_menu_string`. A raw `ca_put(DBR_STRING,"Specified")` to `sel.SELM` thus stored 1, not 0 — the same mis-map as db-load, on the runtime path.
C ref: `dbConvert.c` `putStringMenu` (:1206-1226) resolves a DBR_STRING write against the field's own menu (`strcmp` over `papChoiceValue`, then `epicsParseUInt16(dbConvertBase)`).
Reachability: narrow but valid — standard clients (`caput-rs`, `caput`, pyepics) resolve enum labels client-side and send an index, so this is reached only by a client that deliberately sends `DBR_STRING` with a label. Still observably divergent vs C, which resolves it correctly.
Fix (done): the three sites share `coerce_write_value`, which resolves a String write to a menu field against the field's own menu (`menu_field_choices`/`shared_menu_choices` → `resolve_menu_field_string`) before falling through to `convert_to`. Pinned by `field_io::tests::write_path_menu_label_resolves_against_field_menu`.

## Audited clean this round (no finding)
- Runtime record-field type-drop via CA/PVA/OUT-link: PREVENTED by write-path coercion (`field_io.rs:643-667`, `:101-127`); the ~93 typed-only `put_field` arms are unreachable with a mismatched type via those vectors. Only the INPUT-link sub-path (R4-3) lacks coercion. (The menu-*label* sub-case of this coercion — distinct from type-drop — was the R4-5 sibling, now fixed.)
- PVA monitor pipeline-credit accounting, CA base dropped-monitor counter, bridge connection/pause-vote state machines: single-owner accounting sound, finalizers cover exits.
- CA String→numeric radix (`value.rs:1163-1259`), integer narrowing, DBF_CHAR signedness, old-DBR promotions: match C `dbConvert`/pvxs `copyOut`. (R4-2 is the *bridge* path only.)

## Round 5 Open Findings (record-processing field-output, 2026-06-16)

Fresh-angle audit of `epics-base-rs/src/server/records/*` `process()` vs C
`std/rec/*Record.c`, on the OBSERVABLE axes: processed field values, alarm
severity/status, DBE monitor masks, UDF/INVALID. R5-1..R5-3 verified at the
C+Rust source by me; R5-4..R5-14 reported by sub-agents (C-cited, pending
independent re-verification before fix).

### R5-1 (CRITICAL, VERIFIED, FIXED e95f4469): `sub`/`aSub` subroutine never runs on the normal process path
`SubRecord::process`/`ASubRecord::process` are empty (`sub_record.rs:148`, `asub_record.rs:213`); they rely on `RecordInstance::subroutine`, which was invoked ONLY in `process_local()` (`record_instance.rs:1903`). The main engine `process_record_with_links_inner` calls `instance.record.process()` directly (`processing.rs:1619`) and bypasses `process_local` (its own comment, `processing.rs:1629-1632`). So sub/aSub ran their SNAM only via the by-name/QSRV-group path (`processing.rs:322`), never on SCAN / event / CA-put-to-PP-INP / FLNK. C runs `do_sub()` every `process()` (`subRecord.c:147`, `aSubRecord.c:223`). Observable: VAL/VALA..VALU and OUTA..OUTU never updated.
**FIXED:** extracted the invocation into a single owner `RecordInstance::run_registered_subroutine()` called from BOTH dispatch paths before `record.process()`. The "move onto the record" alternative is a non-starter (the record cannot own the name→fn registry). Regression test drives a VAL-doubling subroutine through `process_record_with_links` (main path). Distinct, not changed: `processing.rs:3922` is the SIMM simulation path, unreachable for sub/aSub (no SIMM/SIML field). Cascading follow-ons split out below as R5-1b / R5-1c.

### R5-1b (MED, FIXED bf7e4c8f): `sub`/`aSub` subroutine return-status not mapped to VAL/SEVR
`SubroutineFn = Box<dyn Fn(&mut dyn Record) -> CaResult<()>>` carried no return-status channel. C `subRecord.c::do_sub` sets `SOFT_ALARM` when the subroutine returns < 0, and `aSubRecord.c` writes the subroutine's `long` return into `VAL` (`aSubRecord.c:223` `prec->val = status`). Reachable since R5-1: a sub subroutine signalling an error raised no SOFT_ALARM, and an aSub's VAL was whatever the closure wrote, not the return code.
**FIXED structurally:** `SubroutineFn` now returns `CaResult<i64>` (the C `long` status; `Ok(0)` normal, `Ok(n<0)` SOFT_ALARM, `Err` = infra failure that aborts processing) — `register_subroutine` bounds updated across ioc_builder/ioc_app/ca_server (public API). The single owner `run_registered_subroutine` (shared by the main-engine and process_local paths) applies it: `aSub` publishes the status as VAL (overwriting the closure's write), and a negative status raises `SOFT_ALARM` at the record's BRSV via `rec_gbl_set_sevr`, before checkAlarms so a higher analog severity still wins by the raise-only rule (BRSV defaults to NO_ALARM → no-op). New BRSV menuAlarmSevr field on both `sub` and `aSub`. Distinct, not changed: C `do_sub` recomputes `udf` only on the status>=0 branch, whereas the framework's `clears_udf()` recompute is unconditional — a marginal, framework-wide approximation (status<0 + finite VAL leaves udf=false either way), not a sub/aSub-specific divergence. Tests: sub status<0→SOFT_ALARM/Major@BRSV + status==0 control; aSub VAL==return code (42 not 999), negative carries to VAL(-5)+SOFT_ALARM/Minor; BRSV round-trip on both.

### R5-1c (MED, FIXED d51ba786): `sub` limit-alarm fields (HIHI/HIGH/LOLO/LOW/HYST/MDEL/ADEL) absent
C `subRecord.c::checkAlarms` (319-373) runs the analog limit check against HIHI/HIGH/LOLO/LOW (with HYST + LALM) and `monitor` (386-394) posts VAL via the MDEL/ADEL deadbands against MLST/ALST. `SubRecord` carried only VAL/SNAM/INP/A — no limit or deadband fields — so a sub never entered HIHI/HIGH/LOLO/LOW alarm and always posted VAL (no MDEL/ADEL gating). Reachable since R5-1.
**FIXED structurally** by routing `sub` through the existing shared owners instead of reimplementing: `sub_record.rs` gains the record-owned trackers MDEL/ADEL/LALM/MLST/ALST (get/put/field_list + `init_record(0)` seeds MLST/ALST/LALM from VAL per subRecord.c:130-132); HIHI/HIGH/LOLO/LOW + HxSV/LxSV + HYST stay framework-common. `record_instance.rs` adds `sub` to the `AnalogAlarmConfig` allocation whitelist and the `evaluate_alarms` analog dispatch arm, so `evaluate_analog_alarm` (UDF early-return, hysteresis, LALM writeback) and the universal `check_deadband_ext` now apply. `sub` is not AFTC-capable (no AFVL in subRecord.dbd) so the AFTC filter is skipped by construction. Tests: HIHI fires MAJOR/HIHI_ALARM with LALM pinned to the threshold + under-HIHI control; MDEL boundary gates the VAL monitor (MLST frozen below MDEL, advances at/over); field round-trip + init seed.

### R5-2 (HIGH, VERIFIED, FIXED e4ae8906): calc/calcout `VAL` token in CALC/OCAL always reads 0
`calc.rs:674`/`calcout.rs` build `NumericInputs::with_vars(vars)` (`prev_val: 0.0`, `engine/mod.rs:100`) and never assign `inputs.prev_val = self.val`; the engine `FetchVal` reads `prev_val` (`engine/numeric.rs:37`). C `calcPerform.c:73-74` `FETCH_VAL: *++ptop = *presult` reads the previous VAL. Trigger: `CALC="VAL+1"` → C 1,2,3…; Rust 1,1,1…. Same `with_vars(prev_val=0)` pattern in swait/scalcout/transform (synApps crate, out of base scope — note as sibling).
**FIXED:** seed `prev_val` before each eval — calc.rs cached-RPCL + fallback paths seed `self.val`; calcout.rs CALC seeds `self.val`, OCAL seeds `self.oval` (C `presult = &oval`, calcoutRecord.c:621). Regression tests assert `VAL+1`/OCAL `VAL+1` count up. swait/scalcout/transform sibling in the synApps crate remains OPEN (out of base-crate scope).

### R5-3 (HIGH, VERIFIED, FIXED 207f5484): fanout/seq SHFT default is 0; C dbd `initial("-1")`
`fanout.rs:93`/`seq.rs:173` default `shft: 0`; C `fanoutRecord.dbd.pod:133`/`seqRecord.dbd.pod:287` `field(SHFT,DBF_SHORT){ initial("-1") }` (POD: "If not set, SHFT is -1 so bits shift left by 1"). Rust db_loader applies no dbd `initial()`, so the struct `Default` is the only source. Trigger: `SELM=Mask`, SHFT omitted → C `seln<<1` (SELN=1 → LNK1), Rust `seln>>0` (SELN=1 → LNK0): wrong forward links fire.
**FIXED:** fanout/seq `Default` now `shft: -1`. `select_link_indices_ex` already implements the signed shift (`shft>=0 ? seln>>shft : seln<<-shft`, mod.rs:392-396), so only the default was wrong. mbbi/mbbo/mbbi_direct/mbbo_direct SHFT have NO dbd `initial()` (default 0) → their `shft: 0` already matches, not changed. Tests assert the -1 default + SELN=1 selects slot 1 under Mask.
**Larger family (now dispositioned):** ANY field whose Rust `Default` ≠ its C dbd `initial()` (db_loader ignores dbd initials) — the separate audit **R5-15** below; all enumerated divergences are FIXED or kept-by-decision, with two documented residuals (no table-driven rewrite; reverse-direction sub-audit owed).

### R5-4..R5-14 (REPORTED by sub-agent, C-cited, pending re-verify)
- R5-4 (MED, FIXED 2b3a66a8): ao closed-loop Incremental DOL adds to current VAL not PVAL (`processing.rs:1398` vs `aoRecord.c:447-455` `val=pval` then `+=`). FIXED: Incremental branch reads `get_field("PVAL")` (OIF is ao-only → PVAL always present). Exposed that test-constructed ao records skip `init_record` (PVAL stays 0); the regression + the updated `test_ao_oif_incremental` now call `init_record(0)` (C init seeds `pval=val`). Reg test: caput VAL=100 between cycles → next increment bases off PVAL=10 (=>15), not 105.
- R5-5 (MED, FIXED 3e812d32): constant DOL re-applied to VAL every process for all 9 OMSL records, clobbering client caputs (`ao.rs:455-457` + framework `processing.rs` resolving `ParsedLink::Constant` each cycle). C gates the per-cycle fetch on `!dbLinkIsConstant` (`aoRecord.c:181-187` `else { value = prec->val; }`): a constant DOL binds VAL once at init (`recGblInitConstantLink`) and is never re-sourced — so OIF=Incremental on a constant DOL does NOT accumulate (the originally-reported "loses the increment, behaves Full" is in fact C-correct; C never increments a constant DOL). FIXED structurally: framework `dol_info` returns `None` for `ParsedLink::Constant` (closes the family at one gate); `recGblInitConstantLink`-parity seeding added to dfanout/longout/int64out/stringout/lso (new `init_record`), mbbo (seed state index before `convert()`), mbbo_direct (`post_init_finalize_undef` seeds VAL + clears UDF so B0..B1F decompose); removed the per-cycle constant re-stamp from `ao::process`/`bo::process` (both already seed at init). Tests: caput survives reprocess on record-removal (ao), framework-only (longout), string (stringout) paths; constant+Incremental stays constant (ao); init applies across f64/i64/long-string/state-index/bit-field types.
- R5-6 (MED, FIXED 70e7d3ed): calcout ODLY>0 posted VAL/OVAL monitors + fired FLNK on the delaying cycle (`calcout.rs:1043` returned `complete_with`), so VAL/OVAL posted ODLY-seconds early and FLNK fired twice (`calcoutRecord.c:277-282` `return 0` before `monitor()`/`recGblFwdLink()` at 306-307). FIXED: the delaying cycle returns `AsyncPendingNotify(["DLYA"=1])` + ReprocessAfter — the framework posts only DLYA, defers the FLNK + VAL/OVAL snapshot, and runs the continuation; the `dlya==1` branch returns Complete on the delayed cycle so FLNK/VAL/OVAL fire once there. (By-name `process_record` never handled ODLY — it drops all ProcessActions — so only the main engine path applies.) Test drives the delayed cycle via `process_record_continuation`: FLNK target unprocessed on the delaying cycle, processed exactly once on the delayed cycle.
- R5-7 (MED, FIXED d14764dd): sel `Specified` mode fetched ALL inputs, not only INP[SELN] (`sel.rs:663` vs `selRecord.c:411-438`): extra input monitors + spurious SEVR from non-selected broken links. FIXED: the framework resolves the sel NVL link into the selector BEFORE the multi-input fetch and restricts the fetch list via a new `select_input_links(selector)` record hook (default `None` = fetch every link; sel returns only INP[SELN] in `Specified` mode, all inputs for High/Low/Median, nothing for SELN >= SEL_MAX — matching C's bounds check). The SELN write-back is unchanged; only the read-only NVL resolution moved earlier. Tests: `sel_specified_mode_fetches_only_the_selected_input` (non-selected PP source stays at default 0, selected source processed to 42); control `sel_high_mode_fetches_all_inputs` (every PP source processed).
- R5-8 (MED, FIXED b9a00f66): sel ran `do_sel` unconditionally; C gates on fetch success (`sel.rs:323` vs `selRecord.c:114`). In Specified mode a configured-but-unresolved selected input (or NVL link) made VAL recompute from the NaN-initialised input field (VAL→NaN) instead of freezing at its last value (observable when a selected link is broken at startup). FIXED structurally: the framework fetch phase computes the Specified-mode gate (fail iff NVL configured+unresolved, OR the non-empty selected input did not reach `resolved_link_fields`; an empty link is NOT a failure — C `dbGetLink` on an unset link returns success) and reports it via a new `Record::set_fetch_gate_failed` hook (default no-op); sel consumes it as a one-shot and freezes VAL/UDF in Specified mode (UDF derives from VAL, so it freezes too). High/Low/Median are deliberately NOT gated — C's "freeze when the LAST link (INPL) fails" is a reference-side quirk of its overwritten `status` (it ignores INPA..INPK failures), not replicated. By-name dispatch never reports a fetch outcome → flag stays clear, no regression. Tests: broken selected link freezes VAL; broken NVL freezes VAL; empty selected link still computes (VAL=NaN, control).
- R5-9 (MED, FIXED 1a453c6c): seq never wrote the DOLn read-back into DOn nor posted DOn (`links.rs:1415` vs `seqRecord.c:256-268`): a client reading/monitoring DO0..DOF saw a stale value. FIXED in the `MultiOut::Seq` dispatch arm: each processed group reads DOLn into `new_dov` (failed/constant read leaves DOn unchanged, per `dbGetLink`), drives LNKn with `Double(new_dov)` when LNKn is real, then posts DOn via `post_fields` (write + DBE_VALUE|DBE_LOG) only when `new_dov` changed (field already holds the old value). Also fixed the group-inclusion rule (`seqRecord.c:182-189`): a group is processed when LNKn OR DOLn is a real (non-constant) link, so a DOL-only group (real DOLn, empty LNKn) now still reads back and posts DOn — previously skipped on the empty-LNK guard. Tests: real-LNK+real-DOL reads DOL0→DO0; DOL-only group with empty LNK0 still updates DO0.
- R5-10 (MED, FIXED 7ac34421): dfanout OUT-link write failure does not raise LINK_ALARM/MAJOR (`links.rs:1362` vs `dfanoutRecord.c:311`). FIXED structurally (same-cycle reorder): C `push_values` raises `recGblSetSevr(LINK_ALARM, MAJOR_ALARM)` per failed `dbPutLink` (312/324/333) and runs BETWEEN `checkAlarms` and `recGblResetAlarms` (127-147), so the write alarm folds into THIS cycle's committed SEVR + VAL post. `write_db_link_value` now returns a failure bool (C `dbPutLink` status != 0); `dispatch_multi_output` gains a phase arg — `Some(nsev)` drives dfanout PRE-commit (gates the push on pending `nsev`, accumulates write failures, returns the single pending alarm — LINK/MAJOR on failure or SOFT/INVALID for Specified `seln` out of range), `None` drives fanout/seq in the post-commit tail; each type dispatches in exactly one phase. `process_record_with_links_inner` runs the dfanout dispatch just before `rec_gbl_reset_alarms` (lock released for the writes, returned alarm folded into `nsev` before the commit). Distinct, not changed: C `seq` (`seqRecord.c:264`) `dbPutLink` captures no status / raises no write alarm; fanout drives forward links only; the other C LINK_ALARM sites (ao/bo/mbbo/mbboDirect DOL read, compress INP read) are input-side LINK/INVALID, a different family. Tests: broken OUT link → SEVR=MAJOR/STAT=LINK after one process (same-cycle); valid link → no alarm + value lands; `dfanout_specified_out_of_range_raises_invalid` still passes (range alarm folds through the new path).
- R5-11 (LOW, FIXED d1f41c82): ai `RVAL` posts on the alarm-transition cycle used a fixed `aux_mask` (forced `DBE_VALUE | DBE_LOG`) rather than VAL's `monitor_mask` (`aiRecord.c:460-465` posts `db_post_events(&prec->rval, monitor_mask)` nested in `if (monitor_mask)`). Under a non-default MDEL the raw count (RVAL) can change while VAL stays inside the deadband, so C is silent on RVAL that cycle and posts `DBE_ALARM` alone on an alarm-only cycle; the port over-notified a direct ai.RVAL monitor with the wrong mask. FIXED: new `Record::fields_posted_with_value_mask()` hook (ai → ["RVAL"]); the shared sub_updates loop (4 copies) posts those fields with VAL's `monitor_mask`/`deadband_mask`, gated on `changed && !deadband_mask.is_empty()` (the `if (monitor_mask)` guard). The originally-listed ao/calc/calcout are NOT-REAL: their aux posts carry the C-forced `monitor_mask | DBE_VALUE | DBE_LOG`, which subsumes any MDEL/ADEL-derived VALUE/LOG bits (DBE_LOG == DBE_ARCHIVE), so a fixed `DBE_VALUE|DBE_LOG` aux_mask is byte-identical to C on those records. Distinct, left on the forced-mask path: ao RVAL/RBV, mbbo/mbboDirect/mbbiDirect RVAL/RBV, sel SELN, compress NUSE. Tests: ai MDEL>0 RVAL-within-deadband not posted; alarm-only cycle posts RVAL DBE_ALARM-only; updated `test_per_field_masks_narrow_deadband_and_aux_posts`.
- R5-12 (LOW, FIXED 0d2b9459): ai writes `LALM=NaN` / drifts AFVL on a UDF cycle; C `checkAlarms` returns early on `prec->udf` (`recGblSetSevr(UDF_ALARM,udfs); [afvl=0;] return;`), leaving LALM untouched and zeroing AFVL on the AFTC-capable records. FIXED structurally at the single framework owner `evaluate_analog_alarm` (reached by all four dispatch paths via `evaluate_alarms`): a UDF early-return at the top zeroes AFVL on ai/longin/int64in/calc and returns before the range check, so LALM no longer drifts to `val` (NaN) and AFVL is not filtered. The guard is uniform across the whole shared arm — verified at `aiRecord.c:319-323`, `aoRecord.c:383-386`, `longinRecord.c:274-278`, `longoutRecord.c:317-320`, `int64inRecord.c:267-271`, `int64outRecord.c:298-301`, `calcRecord.c:300-304`, `calcoutRecord.c:563-566` (out records carry no AFVL and just return). Both LALM and AFVL are client-readable fields. Test: ai UDF cycle (NaN val) leaves seeded LALM, zeroes seeded AFVL, commits UDFS/INVALID.
- R5-13 (LOW, FIXED 7c167cd2): waveform/aai/aao ignored MPST/APST "On Change" and never computed HASH (default "Always" matched). C `monitor()` (waveformRecord.c:291-326) hashes the array content (`epicsMemHash` over `nord * dbValueSize(ftvl)` native bytes) and posts DBE_VALUE/DBE_LOG either always or only on a hash change, plus posts HASH (literal DBE_VALUE) on the change. FIXED faithfully: ported `epicsMemHash` byte-exact (signed-char, LE — unit-tested against the verbatim C function); `WaveformRecord::array_content_bytes` serializes NORD elements to native LE per FTVL; new `Record::array_monitor_post` hook (consulted by `check_deadband_ext`, the single VAL-mask owner) returns the (post_value, post_archive, hash_changed) decision and updates HASH; HASH is event-driven via new `Record::event_posted_fields` (excluded from generic change-detection — no spurious Always-mode post, no double-post) + a `RecordInstance::array_hash_changed` flag driving a literal `("HASH", DBE_VALUE)` post in all four snapshot builders; HASH (DBF_ULONG) exposed via get_field for the three kinds; subArray excluded. Tests: On Change gates VAL + posts HASH (==epicsMemHash(i32[1,2,3])) + suppresses on unchanged + reposts on new content; Always never posts HASH; epicsMemHash byte-exact vs C.
- R5-14 (LOW, FIXED 051d09d9): three re-verify sub-items, one was a live divergence. **Item 1 (FIXED):** seq read SELL→SELN in SELM=All. `dispatch_multi_output` resolved SELL unconditionally for fanout/dfanout/seq — correct for fanout (`fanoutRecord.c:103`) and dfanout (`dfanoutRecord.c:126`), whose `dbGetLink(&sell, DBR_USHORT, &seln)` runs before the SELM switch every cycle, but wrong for seq: `seqRecord.c:148` places the identical dbGetLink *inside* the `else` of `if (selm == seqSELM_All)`, so an All-mode seq never refreshes SELN. The port spuriously updated SELN (and would post a SELN monitor) from a live SELL link in All mode. Gated the seq SELL read on SELM != All (seqSELM_All == 0); fanout/dfanout stay unconditional. Boundary test: All freezes SELN, Specified refreshes it (same live SELL). **Item 2 (already closed, now pinned):** dfanout OMSL/IVOA already served as DBR_ENUM since the menu-serving owner (83236635, 2026-06-10, *predates* this 2026-06-16 finding) — dfanout's `menu_field_choices` overrides only SELM, so OMSL/IVOA fall through to `shared_menu_choices` and `attach_menu_enum` promotes Short→Enum + attaches menuOmsl/menuIvoa labels on GET/MONITOR/create-channel. `test_snapshot_dfanout_omsl_ivoa_serve_as_enum` locks it; the sub-agent report was a false positive (read get_field's Short without the serving promotion). **Item 3 (already closed):** seq DOLn IS coerced to DBR_DOUBLE — the DOLn read-back (R5-9, 1a453c6c) reads via `.to_f64()`, exactly C `dbGetLink(&dol, DBR_DOUBLE, &dov)`. No change.

### R5-15 (MED, STRUCTURAL, enumerated divergences DISPOSITIONED; two residuals documented): db_loader applies no dbd `initial()`; any Rust `Default` ≠ C dbd `initial()` diverges
Surfaced by R5-3 (SHFT). The Rust port hand-codes each record's `Default` impl and never parses the C `.dbd.pod` `field(...){ initial("...") }` directives, so any field whose hand-coded default differs from its C dbd initial() ships the wrong post-load value when the `.db` file omits that field. R5-3 was one instance (SHFT). Audit: enumerated every non-zero / non-empty `initial("...")` in `epics-base/modules/database/src/std/rec/*.dbd.pod` and compared against the corresponding Rust record `Default`. User chose option (a) — correct each diverging Default by hand — over (b) a table-driven dbd-initial rewrite.

**Per-field disposition (all enumerated divergences):**
- **SELN** fanout/dfanout/seq `initial("1")` — FIXED 448d2ab5 (`seln: 0 → 1`; dfanout Specified output is `seln - 1`).
- **OOCH** longout `initial("1")` (YES) — FIXED 5e3c4a77 (`ooch: 0 → 1`; drives the first-output-change gate in `special()`).
- **NELM** histogram `initial("1")` — FIXED 562c1ca9 (`nelm: 10 → 1`; val/wdth auto-derive).
- **NSAM** compress `initial("1")` — FIXED cff05afc (`nsam: 10 → 1`, `val` len follows).
- **SIZV** lsi/lso/printf `initial("41")` — KEPT 256, documented as an intentional deviation 317eb897 (user decision; 256 is the port's chosen long-string capacity).
- **MALM** subArray `initial("1")` — FIXED **structurally** 33634b5d. The naive `0 → 1` was blocked by per-put NELM/INDX-vs-MALM clamping (load-order dependent). C floors MALM to 1 (`subArrayRecord.c::init_record:96-97` — MALM is never 0) and clamps NELM→MALM / INDX→MALM-1 at init (96-104) and at process (310-314). Fix removed the invented `malm==0 ⇒ no-cap` sentinel, moved clamping to a post-load `init_record` + `set_val` (the readValue equivalent), making the .db field order irrelevant by construction.
- **SSCN** all 21 records `initial("65535")` — FIXED **structurally** eb7ce024. `ScanType`'s `from_u16` `_ => Passive` collapsed the 65535 "use SCAN" sentinel to Passive(0). Since `ScanType` is shared with SCAN (always a valid 0-9 index), added a dedicated `SimModeScan` type (a menuScan choice OR `DoNotUse`==65535) so the sentinel is unrepresentable in SCAN; default `DoNotUse`, served as DBR_ENUM(65535). SSCN has no processing effect in the port (sim-mode scan via SSCN unimplemented) — read-back/round-trip value only.

**Residual 1 (tech-debt, documented):** option (b) declined — Default and dbd remain two sources of truth, so a *future* record can re-introduce drift. Not a current divergence.

**Residual 2 (sub-audit owed):** the enumeration was one-directional (C non-trivial `initial()` → compare Rust). The reverse — Rust `Default` non-zero where C `initial()` is zero/absent — was not run exhaustively. One instance already seen: histogram `ULIM=10`/`LLIM=0` (C initial 0). Latent fields correctly matching C and **not to be touched**: calcout `INAV..INUV` (Rust=3, matches C), and the unmodeled SDLY / aSub EFL,ONV.

### R5 audited clean (sub-agent verified, no divergence)
ai convert (RVAL+ROFF/ASLO+AOFF/LINR/ESLO+EOFF/SMOO all quadrants/HIHI-LOLO order+HYST+LALM/AFTC); ao convert (drive clamp/OROC/linearization/raw round/IVOA/RVAL+RBV masks); calc numeric domain (div/sqrt/log/mod/nint/atan2/NaN→UDF, OOPT 6 modes, DOPT, IVOA/IVOV, LA..LU); compress (all per-ALG arithmetic, ILIL/IHIL, PBUF tail, FIFO/LIFO+lin, RES); sel selection (High/Low/Median incl. even/odd index, all-NaN, out-of-range SOFT, HYST+LALM); fanout/dfanout/seq (SELM=All/Specified/Mask ordering+range, dfanout IVOA/checkAlarms/MDEL/ADEL/DOL, fanout Passive FLNK gate); waveform (UDF, NORD, subArray clamps, default MPST/APST).

## Review Log
- Round 4 (2026-06-16): commit-history classification (11 recurring families) + 4-agent code hunt. 4 families have open siblings → R4-1 (menu-label, 12 menus + mis-map; the dominant one, structural), R4-2 (QSRV radix), R4-3 (compress INP coercion), R4-4 (trap-write AfterWrite, 3 sites). The classification and the independent code hunt converged on the same open set.
- Round 5 (2026-06-16): fresh-angle record-processing field-output audit (alarm order, monitor masks, value edge cases, UDF). Found the port is NOT converged on record behaviour — R5-1 (sub/aSub subroutine inert on the normal process path, CRITICAL) + R5-2 (calc/calcout VAL token reads 0) + R5-3 (fanout/seq SHFT default) verified at source; R5-4..R5-14 reported by sub-agents pending re-verify. Contrast with the R4-4 finalizer-skip sweep, which found that family isolated — the record-output axis is where live divergences remain.
- Round 4 fixes (2026-06-16): R4-1 db-load menu-label resolution FIXED (e7e20583). The [Fixes from reported defects] enumeration of the same root (field-blind `resolve_menu_string`) surfaced a runtime-write sibling on the `field_io` coercion path → filed + FIXED as R4-5 (86137c83). Both reuse the shared `resolve_menu_field_string` helper. R4-2 QSRV String→int base-0 radix FIXED (90167988, shared `EpicsValue::parse_int`/`parse_uint`). R4-3 compress INP-link coercion FIXED (f388beab, `put_field_internal` coerces to the target field type). R4-4 trap-write AfterWrite-on-every-exit-path FIXED (02053a3d) — verifying the C reference (`camessage.c:1400/1620/1700` all call `asTrapWriteAfter`) confirmed the divergence and dictated the structural fix: a `TrapWriteGuard` RAII pair (BeforeWrite on `begin`, AfterWrite on `complete`-or-`Drop`) replacing the three explicit dispatch sites across epics-base-rs/epics-bridge-rs/epics-ca-rs. **All round-4 findings (R4-1..R4-5) now closed.**
