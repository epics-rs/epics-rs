# Expected-deviation allowlist for the differential oracle.
#
# WHAT THIS IS
# Under the product policy adopted 2026-07-13 (doc/strategy-2026-07-13.md §2),
# "C's bugs are not the contract. Clean is the goal." So a difference between
# the C softIoc and the Rust port is NOT automatically a port bug. It is one of
# these things:
#
#   * a deliberate refusal to reproduce a C defect     -> EXPECTED DEVIATION
#   * an intentional port DESIGN divergence            -> EXPECTED DEVIATION
#   * a ground-truth INSTRUMENT superset artifact      -> EXPECTED DEVIATION
#   * anything else                                     -> PORT DEFECT
#
# THREE JUSTIFICATION BASES, FOUR BUCKETS
# Most rows are C-bug refusals: for these the allowlist IS doc/upstream-c-bugs.md.
# Each NOT-REPRODUCED (or, disabled, REPRODUCED) row transcribes an entry from
# that catalogue and MUST cite the CBUG id that justifies it; if such a row is
# not backed by an entry there, the row is wrong.
#
# A DESIGN-DIVERGENCE row is different in kind: it records an INTENTIONAL port
# design choice that is neither a C-bug refusal nor a defect (e.g. lifting a
# fixed-capacity buffer cap and serving live length). The port deliberately does
# something other than the spec.
#
# An INSTRUMENT-SUPERSET row is the OPPOSITE direction: the port matches the
# measured spec, but the PVA ground-truth INSTRUMENT exceeds it. softIocPVX loads
# softIocPVX.dbd, a superset of the oracle's measured softIoc.dbd, so it serves
# extra menu choices (pvxs's own example device support) the shared spec never
# declares. The port is not pvxs and correctly serves the measured spec; the
# difference is the instrument's, not the port's.
#
# Both DESIGN-DIVERGENCE and INSTRUMENT-SUPERSET are justified by their `why` —
# the rationale stated in place — and cite NO CBUG. The CBUG requirement is
# relaxed for these two buckets only; every row still needs a non-empty, unique
# `id` (it keys the fired/stale ledger) and a non-empty `why`. Buckets are
# validated at load (allowlist.rs::parse); an unknown bucket string is a parse
# error, not a silent wildcard. All four buckets match, fire, and go stale by the
# same rules — the bucket changes only what justifies a row.
#
# SELF-CHECKING, IN BOTH DIRECTIONS
#   * a diff that matches a row      -> reported as EXPECTED DEVIATION, not a failure
#   * a diff that matches no row     -> reported as a PORT DEFECT
#   * a row that STOPS firing        -> reported as STALE. The deviation vanished.
#                                       Either the port regressed back onto C's
#                                       bug, or C fixed it upstream. Both are
#                                       findings; neither may pass silently.
#
# MATCHING
# A row fires when every constraint it states holds. Omitted constraints are
# wildcards, so a row is as narrow as it is written. `record_types` and `fields`
# are exact-match lists; `classes` matches the boundary class from cases.rs;
# `surface` matches which observation differed.
#
# SCOPE NOTE, stated rather than implied: most of the 76 NOT-REPRODUCED entries
# in the catalogue are Tier-3 (asyn, ADCore, motor, optics, modbus) or live
# inside the calc expression engines. They cannot fire on the record/field CA
# surface this harness drives, so they are deliberately NOT transcribed here --
# listing them would be padding, and a row that can never fire would be flagged
# STALE forever. Only entries reachable from a CA get/put on a base record
# field are listed. When the harness grows a calc-expression generator, the
# CBUG-A* / CBUG-C* / CBUG-F1..F5 rows become reachable and belong here.

schema = 1

# --------------------------------------------------------------------------
# Reachable today: base record/field CA surface.
# --------------------------------------------------------------------------

[[deviation]]
id = "CBUG-F6"
bucket = "NOT-REPRODUCED"
upstream = "epics-base / calc"
record_types = ["calc", "calcout"]
fields = ["INPM", "INPN", "INPO", "INPP", "INPQ", "INPR", "INPS", "INPT", "INPU"]
surface = ["put_accepted"]
why = """
C's calcRecord.dbd declares special(SPC_MOD) on the nine link fields INPM..INPU,
but calcRecord.c's special() implements only SPC_CALC and falls through to
S_db_badChoice. So on a C IOC every dbPutField to these nine documented fields is
REJECTED -- they are unwritable over CA. The port implements them as ordinary
writable links, so the put is ACCEPTED. The deviation is the port refusing to
reproduce an upstream defect that makes documented fields unusable.
Expect: C rejects the put, port accepts it.
"""

[[deviation]]
id = "CBUG-E1"
bucket = "NOT-REPRODUCED"
upstream = "epics-base / db"
record_types = ["compress"]
fields = ["VAL"]
surface = ["value_string", "value_numeric", "monitor_events"]
why = """
In C, a scalar dbPut into a compress record's VAL takes its write offset from
get_array_info(), which returns the READ start (it exists to serve dbGet). In FIFO
mode that is the oldest element, so successive client puts overwrite the same slot
instead of appending: three puts into an empty NSAM=3 FIFO leave [3,0,0] with NUSE
still 0. The port appends at the write cursor and bumps NUSE, giving [1,2,3].
One get_array_info serves two callers with opposite needs; dbPut consumes dbGet's
answer.
Expect: C and port disagree on VAL contents/NORD after scalar puts into compress.
"""

[[deviation]]
id = "CBUG-E2"
bucket = "NOT-REPRODUCED"
upstream = "epics-base / dbConvert"
fields = []
# The row's scope is the DESTINATION type, not a place: dbConvert instantiates
# the same PUT macro body for every integer destination and for epicsEnum16, on
# every record type. A float/double destination takes no integer cast and is
# deliberately absent -- ai.VAL is DBF_DOUBLE, so `inf` vs `0` there is not this
# bug and stays a DEFECT until adjudicated on its own.
dbf_types = [
    "DBF_CHAR",
    "DBF_UCHAR",
    "DBF_SHORT",
    "DBF_USHORT",
    "DBF_LONG",
    "DBF_ULONG",
    "DBF_INT64",
    "DBF_UINT64",
    "DBF_ENUM",
    "DBF_MENU",
]
surface = ["value_string", "value_numeric"]
# `over-double-max`, `over-float-max` and `nan` are NOT here: cases.rs generates
# those three only for DBF_FLOAT/DBF_DOUBLE fields, which `dbf_types` excludes,
# so listing them would restate a scope the row does not have.
classes = ["over-max", "under-min", "enum-negative-ordinal"]
why = """
dbConvert's double->integer put is a bare C cast, which is UNDEFINED out of range
(C17 6.3.1.4p1) -- so compiled C is not single-valued. The C softIoc this harness
diffs against is x86-64, where cvttsd2si yields the integer indefinite
(3.0e9 -> INT_MIN, NaN -> INT_MIN); an aarch64 IOC's fcvtzs SATURATES on the same
input (3.0e9 -> INT_MAX, NaN -> 0). There is no single C answer to be faithful to.

Adjudicated 2026-07-14 (commit 651bf392): the port SATURATES and sends NaN to 0
(types::c_cast), which is byte-identical to a compiled aarch64 IOC. No alarm --
the stored value is in range and valid, and dbConvert runs outside the record's
process cycle where an alarm would be erased by the next recGblResetAlarms.

The epicsEnum16 (DBF_MENU/DBF_ENUM) destination is the same cast: `putDoubleEnum`
instantiates the identical PUT macro body, so a numeric ordinal put whose value is
out of the u16 range takes the same UB. `caput` sends a numeric-but-not-a-choice
value to an enum field as DBR_DOUBLE (caput.c:498-507), so `caput SCAN -1` reaches
`putDoubleEnum` as -1.0: the x86-64 softIoc stores the indefinite 65535, the port
saturates the negative double to 0 -- a valid menu ordinal (SCAN -> Passive,
PINI -> NO, a severity -> NO_ALARM). That is the `enum-negative-ordinal` class. Its
value-agreeing twin -- a negative ordinal into a *signed* raw field (PRIO's i16)
that holds -1 exactly -- produces no value diff and is not matched here. (An enum
put whose ordinal is IN u16 range but out of the record's menu range is NOT this
cast -- it is exact -- and is adjudicated separately, not under E2.)

Expect: on an out-of-range numeric put, the x86-64 C softIoc stores the indefinite
value and the port stores the saturated one. In range, both agree exactly.

Scope: the destination's declared DBF type (dbf_types above), because that is what
selects dbConvert's routine. A DBF_FLOAT or DBF_DOUBLE destination takes no integer
cast and nothing here justifies a difference on one.
"""

[[deviation]]
id = "CBUG-G1"
bucket = "NOT-REPRODUCED"
upstream = "pvxs / QSRV2"
record_types = ["asyn", "bo", "busy", "mbbiDirect", "mbboDirect", "sseq", "transform"]
fields = []
surface = ["value_marking", "seed_event"]
port_adds_leaves = ["display.precision"]
why = """
pvxs QSRV2 nests the display.precision assignment inside the DBR_GR_DOUBLE branch
(ioc/iocsource.cpp:288-292): a field that supplies get_precision but NULLs
get_graphic_double never gets its precision served over PVA, even though DBR_PRECISION
and DBR_GR_DOUBLE are independent rset slots and CA serves it. The seven record types
here are exactly the fat-dbd types whose rset supplies get_precision while NULLing
get_graphic_double (bo/mbbiDirect/mbboDirect in base; busy/asyn/transform/sseq in the
modules). The port declines to reproduce the drop -- nt::qsrv_marks::property_leaves
gates display.precision on its own DBR_PRECISION slot -- so the port's pvxget reply
carries a display.precision leaf (bo.HIGH = 2 = boHIGHprecision; the rest = 0, the
recGblGetPrec default) that pvxs omits. The value the port serves is the same one CA
serves, so this is PVA-vs-CA consistency, not a new number.

The same add shows on the monitor SEED (the initial full update carries the same
getProperties leaves), so the row covers both surfaces: value_marking (read) and
seed_event (monitor). transform.VAL is the one family VAL a monitor drives.

Content-constrained (port_adds_leaves): a case matches ONLY when the sole marking
difference is the port adding one or more display.precision lines pvxs omitted, with
nothing else on the channel changed or removed. Any other marking difference on these
types stays a DEFECT — e.g. mbboDirect.VAL's pre-existing seed/update difference is
not laundered.

Expect: on these types' numeric fields, the port's pvxget (and monitor seed) marks
display.precision and softIocPVX does not; the two renderings are otherwise identical.
"""

# --------------------------------------------------------------------------
# DESIGN-DIVERGENCE entries: intentional port design choices, justified by the
# `why` in place rather than by a CBUG. These match/fire/go-stale like any other
# row; they simply cite no upstream-bug catalogue entry.
# --------------------------------------------------------------------------

[[deviation]]
id = "DESIGN-ASYN-BOUT"
bucket = "DESIGN-DIVERGENCE"
upstream = "asyn / asynRecord"
record_types = ["asyn"]
fields = ["BOUT"]
surface = ["value_marking"]
why = """
asynRecord.dbd declares BOUT as DBF_CHAR special(SPC_DBADDR), a byte array whose
capacity C fixes at OMAX (initial 80) and whose write length is NOWT (initial
80). On a C IOC the field is a pre-sized 80-element buffer, so QSRV2 serves it
zero-filled at init: pvxget reads back epicsInt8_t[80] = {0,0,...} before
anything is written. The port declines to pre-size: it lifts the fixed 80-byte
OMAX cap and serves the ACTUAL written content, so an unwritten BOUT reads back
as an empty epicsInt8_t[0]. This is an intentional design choice, not a C-bug
refusal and not a port defect — the 80-element cap was a static asynRecord.dbd
sizing detail, and the port models the field as a live-length array instead.
There is no CBUG here: nothing on the C side is wrong, the two just made
different, defensible sizing decisions.

Matched on the PVA value/marking contract (pvxget), scoped to exactly asyn.BOUT.
The scope is field-level, so this row justifies only the BOUT length/content
difference and cannot launder a diff on any other asyn channel.

Expect: on ORACLE:ASYN.BOUT the C softIocPVX serves int8[]{80} (zero-filled to
the OMAX cap) and the port serves int8[]{0} (empty until written).
"""

# --------------------------------------------------------------------------
# INSTRUMENT-SUPERSET entries: the PVA ground truth (softIocPVX) loads
# softIocPVX.dbd, a superset of the oracle's measured softIoc.dbd. softIocPVX.dbd
# additionally links pvxs's OWN example device support (pvxs/ioc/pvxs7x.dbd and
# pvxs3x.dbd):
#     device(waveform, CONSTANT, devWfPDBQ2Demo,  "QSRV2 Demo")
#     device(longin,   CONSTANT, devLoPDBQ2UTag,  "QSRV2 Set UTag")
# Those device()s add a DTYP menu choice on the INSTRUMENT that the shared spec
# never declares. The port is not pvxs, has no devWfPDBQ2Demo/devLoPDBQ2UTag, and
# correctly serves the measured spec's DTYP menu — which is why the CA --phase all
# run agrees on these same channels (CA reads softIoc.dbd, not the pvxs superset).
# These rows use the symmetric `ground_truth_adds_choices` content constraint: a
# case matches ONLY when the sole value_marking difference is the ground truth
# carrying EXACTLY the named extra choice on one leaf, nothing else changed or
# removed. Any other DTYP marking difference stays a DEFECT.
# --------------------------------------------------------------------------

[[deviation]]
id = "INSTR-QSRV2-LONGIN-UTAG"
bucket = "INSTRUMENT-SUPERSET"
upstream = "pvxs / QSRV2 example device support"
record_types = ["longin"]
fields = ["DTYP"]
surface = ["value_marking"]
ground_truth_adds_choices = ["QSRV2 Set UTag"]
why = """
softIocPVX.dbd (the PVA ground truth) is a superset of the oracle's measured
softIoc.dbd. It links pvxs's example device support from pvxs/ioc/pvxs7x.dbd
(also pvxs3x.dbd): `device(longin, CONSTANT, devLoPDBQ2UTag, "QSRV2 Set UTag")`.
So softIocPVX's longin.DTYP menu carries an extra "QSRV2 Set UTag" choice that
softIoc.dbd — the shared spec both CA and the port measure — does not declare.
The port has no devLoPDBQ2UTag device support and correctly serves the measured
menu, so its longin.DTYP value.choices omits that one choice. This is a ground-
truth instrument-superset artifact, not a port defect: the CA --phase all run
agrees on longin.DTYP (CA reads the shared spec, not the pvxs superset).

Content-constrained (ground_truth_adds_choices): matches ONLY when the sole
value_marking difference is the ground truth's value.choices carrying exactly the
extra "QSRV2 Set UTag" and every other leaf identical. Any other longin.DTYP
marking difference stays a DEFECT. On this branch (before the separate asyn DTYP
menu fix) the port also omits the asyn menu choices, so the extra set is larger
than the named one and this row does NOT yet fire; it fires once that fix lands
and the QSRV2 choice is the sole residual.

Expect: softIocPVX serves longin.DTYP value.choices including "QSRV2 Set UTag";
the port serves the same menu minus that one instrument-only choice.
"""

[[deviation]]
id = "INSTR-QSRV2-WAVEFORM-DEMO"
bucket = "INSTRUMENT-SUPERSET"
upstream = "pvxs / QSRV2 example device support"
record_types = ["waveform"]
fields = ["DTYP"]
surface = ["value_marking"]
ground_truth_adds_choices = ["QSRV2 Demo"]
why = """
As INSTR-QSRV2-LONGIN-UTAG, for waveform: softIocPVX.dbd links pvxs's example
device support `device(waveform, CONSTANT, devWfPDBQ2Demo, "QSRV2 Demo")` from
pvxs/ioc/pvxs7x.dbd (also pvxs3x.dbd). softIocPVX's waveform.DTYP menu carries an
extra "QSRV2 Demo" choice the shared softIoc.dbd never declares; the port, having
no devWfPDBQ2Demo, serves the measured menu without it. Ground-truth instrument-
superset artifact, not a port defect — CA --phase all agrees on waveform.DTYP.

Content-constrained (ground_truth_adds_choices): matches ONLY when the sole
value_marking difference is the ground truth's value.choices carrying exactly the
extra "QSRV2 Demo" and every other leaf identical. Any other waveform.DTYP
marking difference stays a DEFECT. Same branch note as the longin row: fires once
the separate asyn DTYP menu fix lands and "QSRV2 Demo" is the sole residual.

Expect: softIocPVX serves waveform.DTYP value.choices including "QSRV2 Demo"; the
port serves the same menu minus that one instrument-only choice.
"""

# --------------------------------------------------------------------------
# REPRODUCED entries that touch this surface.
#
# These are NOT deviations today: the port deliberately carries C's bug, so the
# oracle must see AGREEMENT. They are recorded here (disabled) because
# doc/strategy-2026-07-13.md §4 schedules them for re-adjudication under the
# clean policy -- when the port stops reproducing them, these become live rows
# and the diff they produce is EXPECTED, not a defect. Until then, a diff on
# this surface would mean the port drifted off C without a decision, which is a
# finding.
# --------------------------------------------------------------------------

[[deviation]]
id = "CBUG-F12"
bucket = "NOT-REPRODUCED"
enabled = true
upstream = "epics-base / histogram"
record_types = ["histogram"]
fields = ["SGNL"]
surface = ["stat", "sevr"]
why = """
An INVERTED-limits histogram (LLIM > ULIM) cannot bin anything, so the record is
genuinely misconfigured. C's add_count tries to raise SOFT/INVALID but writes
stat/sevr DIRECTLY instead of nsta/nsev, so recGblResetAlarms erases it on the
process path (dead code -> NO_ALARM) while it sticks on the .SGNL special path.
Per strategy-2026-07-13 §2 (clean is the goal) the port now REFUSES this: it raises
the alarm through the single nsta/nsev owner (recGblSetSevr), so a misconfigured
histogram reports SOFT/INVALID CONSISTENTLY on both the process and the .SGNL
special paths (port fix landed 2026-07-20, CBUG-F12 REPRODUCED -> NOT-REPRODUCED;
covered by epics-base-rs `histogram_invalid_limits_alarm` unit tests).

ENABLED 2026-08-03, and the scope corrected, after the first full `--phase all`
run since the port fix landed. Two things were wrong here:

1. The row claimed the harness "never drives LLIM >= ULIM (no such case in
   cases.rs)". False. Neither LLIM nor ULIM carries an `initial(...)` in
   histogramRecord.dbd, so every bare `record(histogram, "…") {}` the harness
   loads already satisfies `LLIM >= ULIM` at 0 == 0 — C's condition holds on the
   DEFAULT record, not only on a configured-inverted one.
2. `fields` named STAT/SEVR and `surface` named value_string. `fields` is matched
   against the case's own field and `surface` against the compared surface, so
   this row could not have matched any case even enabled.

What the run measured: C's behaviour is path-dependent on the one condition, so
NO single port test agrees with C on both paths. With `>=` the port alarmed on
the process path where C erases (14 DEFECTs on SCAN/PROC/UDF, every case a bare
histogram); narrowed to `>` the port is quiet on the default record's SGNL path
where C's sticky direct write shows SOFT/INVALID (10 DEFECTs on SGNL). The `>`
form was taken: the port's rule is then uniform — an INVERSION an operator
created alarms on both paths, an EMPTY range (which is what an unconfigured
record has) alarms on neither — and the residue is exactly C's dead-code/sticky
split, which is what this row exists to allowlist. Those 10 SGNL cases are its
live scope.
"""
