[package]
name = "snc"
version = "0.1.0"
edition = "2021"
description = "SNL compiler CLI for sequencer-rs"

[dependencies]
snc-core = { path = "../snc-core" }
clap = { version = "4", features = ["derive"] }
